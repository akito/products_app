# postgreSQL version 9.6.x
#   gem install pg
#
#   Ensure the SQLite 3 gem is defined in your Gemfile
#   gem 'pg'
#
default: &default
   adapter: postgresql
   encoding: unicode
   pool: 5

development:
   <<: *default
   database: myapp
   username: postgres
   password:

test:
   <<: *default
   database: myapp_test
   username: postgres
   password: